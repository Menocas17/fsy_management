<% content_for :title, "Editar #{@company.name}" %>
<% content_for :eyebrow, "Compañías" %>
<div class="max-w-2xl mx-auto px-3 md:px-7 pt-8">
  <div class="flex justify-end mb-5">
    <%= render(ButtonComponent.new(text: "Atrás", url: @company, type: "cancel")) %>
  </div>
  <%= render "companies/form", company: @company, url: company_path(@company) %>
</div>
