<%
  colors = {
    "logistica" => "bg-cat-amber/20 text-amber-700 dark:text-cat-amber",
    "companias" => "bg-cat-blue/15 text-cat-blue",
    "asignaciones" => "bg-cat-green/15 text-cat-green"
  }
%>
<span class="<%= colors.fetch(log.category, "bg-canvas text-ink-500") %> inline-flex items-center px-2.5 py-0.5 rounded-full text-[11px] font-semibold whitespace-nowrap"><%= log.category_label %></span>
