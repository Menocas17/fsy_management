<%# Assignments are managed from the profile card, where they can be added, confirmed and removed one by one. %>
<% participant = form.object %>
<section class="<%= card_classes("p-6") %>">
  <%= section_heading "Asignaciones", "clipboard-check" %>
  <p class="-mt-1 text-[13px] leading-relaxed text-ink-500 dark:text-slate-400">
    Las asignaciones se agregan desde el perfil: se eligen de la agenda o se crean aparte, con su hora y lugar.
  </p>
  <% if participant.persisted? %>
    <p class="mt-3 text-[13px] font-semibold text-ink-700 dark:text-slate-300">
      <%= pluralize(participant.assignments.count, "asignación registrada", plural: "asignaciones registradas") %>.
    </p>
    <%= link_to participant_path(participant, anchor: "asignaciones"),
          class: "mt-3 inline-flex items-center gap-2 h-10 px-4 rounded-[11px] border border-line dark:border-slate-600 bg-surface dark:bg-slate-800 text-sm font-semibold text-ink-700 dark:text-slate-300 hover:bg-canvas dark:hover:bg-slate-700 transition" do %>
      <%= icon "clipboard-list", class: "w-4 h-4" %>
      Ver asignaciones en el perfil
    <% end %>
  <% else %>
    <p class="mt-3 text-[13px] text-ink-500 dark:text-slate-400">Primero guarda el registro; después podrás asignarle tareas desde su perfil.</p>
  <% end %>
</section>
