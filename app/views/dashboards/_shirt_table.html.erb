<div class="overflow-x-auto shadow-md rounded-xl border border-gray-200 dark:border-slate-700">
  <table class="w-full text-left border-collapse">
    <thead>
      <tr class="bg-primary text-white text-sm tracking-wider">
        <th class="py-3 px-4 font-semibold">Talla de Camisa</th>
        <th class="py-3 px-4 font-semibold text-right">Total Registrados</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-gray-200 dark:divide-slate-700 text-sm">
      <% %w[xs s m l xl].each_with_index do |size, index| %>
        <tr class="<%= index.even? ? 'bg-white dark:bg-slate-800' : 'bg-gray-50 dark:bg-slate-700/40' %> hover:bg-gray-100 dark:hover:bg-slate-700 transition">
          <td class="py-3 px-4 font-bold text-gray-700 dark:text-slate-300 uppercase"><%= size %></td>
          <td class="py-3 px-4 text-right font-medium text-gray-900 dark:text-slate-100">
            <%= shirt_counts[size] || 0 %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>