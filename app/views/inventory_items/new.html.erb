<% content_for :title, "Nuevo artículo" %>
<% content_for :eyebrow, @inventory.name %>
<% content_for :heading, "Nuevo artículo" %>
<% back_to @inventory.name, inventory_path(@inventory) %>
<div class="px-4 md:px-8 pt-6 md:pt-8 pb-10 max-w-3xl mx-auto">
  <%= render "form", item: @item, inventory: @inventory %>
</div>
