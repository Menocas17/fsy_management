<% content_for :title, "Editar #{@inventory.name}" %>
<% content_for :eyebrow, "Inventario" %>
<% content_for :heading, "Editar inventario" %>
<% back_to @inventory.name, inventory_path(@inventory) %>
<div class="px-4 md:px-8 pt-6 md:pt-8 pb-10 max-w-3xl mx-auto">
  <%= render "form", inventory: @inventory %>
</div>
