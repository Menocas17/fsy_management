<% if @is_submit%>
  <button type="submit" class="<%= styles %>">
    <div class="flex gap-1 items-center justify-center">
      <% if @icon %>
        <%= image_tag(@icon, class: "w-[15px] h-[15px]") %>
      <% end %>
      <span><%= @text %></span>
    </div>
  </button>
<%elsif @is_delete%>
  <%= button_to @url, method: :delete, data: { turbo_confirm: "Etas seguro que deseas borrar? Esta accion es irreversible" }, class: styles do %>
    <div class="flex gap-1 items-center justify-center">
      <% if @icon %>
        <%= image_tag(@icon, class: "w-[15px] h-[15px]") %>
      <% end %>
      <span><%= @text %></span>
    </div>
  <% end %>
<%elsif @is_button%>
  <button type="button" class="<%= styles %>">
    <div class="flex gap-1 items-center justify-center ">
      <% if @icon %>
        <%= image_tag(@icon, class: "w-[15px] h-[15px]") %>
      <% end %>
      <span><%= @text %></span>
    </div>
  </button>
<%else%>
  <%= link_to @url, class: styles do %>
    <div class="flex gap-3 items-center">
      <%if @icon%>
        <%= image_tag(@icon, class: "w-[20px] h-[20px]") %>
      <%end%>
      <span><%= @text %></span>
    </div>
  <%end%>
<%end%>
